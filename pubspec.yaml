name: tiket_konser
description: "A new Flutter project for concert tickets."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  dio: ^5.4.0
  provider: ^6.1.1
  google_fonts: ^6.1.0
  intl: ^0.19.0
  file_picker: ^6.1.1
  fl_chart: ^0.66.0
  go_router: ^13.1.0
  http_parser: ^4.0.2
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
