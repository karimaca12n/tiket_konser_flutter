name: tiket_konser
description: "A new Flutter project for concert tickets."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  dio: ^5.4.0
  provider: ^6.1.1
  google_fonts: ^8.0.2
  intl: ^0.20.2
  file_picker: ^10.3.10
  fl_chart: ^1.2.0
  go_router: ^17.2.0
  http_parser: ^4.0.2
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
